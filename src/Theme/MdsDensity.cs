namespace MudShell.Theme;

/// <summary>Layout density for MudShell components. Affects spacing/sizing only — never color, radius, or typography.</summary>
public enum MdsDensity
{
    Comfortable,
    Compact,
}
